/**
 * Class used to run the Wordle game
 */
public class WordleRun {
    /**
     * Creates a Wordle object and prompts the game to start
     */
    public static void main(String[] args) {
        //Making a Wordle object which will start the game
        Wordle game = new Wordle();
        game.setVisible(true); //Allows the window to pop up
    }
}